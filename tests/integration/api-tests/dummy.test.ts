/**
 * Dummy API integration test  guarantees that Jest finds at least one test.
 * This file is deliberately simple and has no external dependencies.
 */
describe('Dummy API integration test', () => {
  test('always passes', () => {
    expect(true).toBe(true);
  });
});
