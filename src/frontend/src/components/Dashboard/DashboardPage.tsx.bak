import React from "react";
import Layout from "../Common/Layout";
import MetricsCard from "./MetricsCard";
import QuickActions from "./QuickActions";
import RecentActivity from "./RecentActivity";
import { mockMetrics, recentConversations } from "../../utils/mockData";

// The dashboard page - functional requirements satisfied without Next.js router
export const DashboardPage: React.FC = () => {
  // Placeholder navigation handlers (can be wired to real router later)
  const navigate = (path: string) => console.log("Navigate to", path);

  return (
    <Layout>
      {/* Notification banner placeholder */}
      <div style={{ background: "#fffae6", padding: "10px", marginBottom: "15px" }}>
        <strong>New Travel Advisory:</strong> Updated COVID-19 restrictions for Europe.
        <button style={{ marginLeft: "10px" }} onClick={() => console.log("Dismiss banner")}>
          Dismiss
        </button>
      </div>

      {/* Search bar */}
      <div style={{ marginBottom: "20px" }}>
        <input
          type="text"
          placeholder="Ask about travel risks, safety guidelines, or destination information..."
          style={{ width: "100%", padding: "8px", fontSize: "1rem" }}
          autoFocus
        />
      </div>

      {/* Main content */}
      <MetricsCard />
      <QuickActions />
      <RecentActivity />
    </Layout>
  );
};

export default DashboardPage;
