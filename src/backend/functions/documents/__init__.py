"""
Documentrelated Azure Function placeholders.

The actual implementation lives in the dedicated subfolders
(upload/, download/, versions/). This file only exists to
make the directory a Python package.
"""
