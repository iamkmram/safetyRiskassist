openapi: 3.0.0
info:
  title: Travel Assistant API
  version: 1.0.0
  description: Minimal API spec for dashboard integration tests
servers:
  - url: https://api.example.com
paths:
  /health:
    get:
      summary: Health check endpoint
      operationId: getHealth
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
